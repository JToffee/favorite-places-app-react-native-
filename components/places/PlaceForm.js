import { View, Text } from "react-native";

function PlaceForm(props) {
	// JSX to allow user to enter a place
	return (
		<View>
			<Text> Add place form coming soon</Text>
		</View>
	);
}

export default PlaceForm;
